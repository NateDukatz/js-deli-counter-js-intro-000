function takeANumber(katzDeliLine, name) {
  for (var i = 0; i <= katzDeliLine.length; i++) {
    if (katzDeliLine[i] = name) {
      return `Welcome, Ada. You are number ${i + 1} in line.`
    }
  }
}